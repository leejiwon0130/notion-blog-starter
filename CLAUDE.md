# 이 프로젝트에서 지켜야 할 규칙

## 🚫 1순위 규칙 — 사용자의 디자인을 절대 덮어쓰지 마세요

이 저장소를 쓰는 사람은 **이미 자기 홈페이지를 만들고 있는 중**입니다.
사람마다 만드는 방식도, 디자인도 전부 다릅니다.

**이 저장소에서 가져갈 것은 「블로그가 돌아가게 하는 기능」 하나뿐입니다.**

### 절대 하지 말 것

- ❌ 이 저장소의 `app/globals.css` 를 사용자 프로젝트에 복사 — **절대 금지**
- ❌ 사용자의 CSS·색상·폰트·레이아웃을 이 저장소 것으로 교체
- ❌ 사용자의 홈(메인) 페이지를 이 저장소 `app/page.js` 로 덮어쓰기
- ❌ 사용자의 헤더·푸터·네비게이션을 이 저장소 `app/layout.js` 로 교체
- ❌ 사용자가 만든 파일을 지우거나 통째로 바꾸기

### 가져갈 것 (기능만)

- ✅ `lib/notion.js` — 노션에서 글을 가져오는 코드 (그대로 사용 가능)
- ✅ 블로그 **목록·상세 페이지의 데이터 처리 로직** (`app/blog/*` 의 로직 부분)
- ✅ `app/sitemap.js` · `app/robots.js` (사용자에게 없을 때만)
- ✅ 의존성 4개: `@notionhq/client` `notion-to-md` `react-markdown` `remark-gfm`

### 화면(마크업·스타일)은 이렇게

블로그 목록·상세 페이지를 새로 만들 때는
**사용자가 이미 쓰고 있는 색·폰트·카드 스타일·클래스 이름을 그대로 따라 하세요.**

1. 먼저 사용자의 기존 CSS와 컴포넌트를 읽는다
2. 거기서 쓰는 색상 변수·폰트·여백 규칙을 파악한다
3. **그 스타일로** 블로그 화면을 만든다

이 저장소의 디자인은 **"이렇게 동작하면 된다"는 참고**일 뿐입니다. 보기만 하세요.

### 작업 전에 반드시

시작하기 전에 사용자에게 **이렇게 확인받으세요.**

> "지금 홈페이지에 블로그 기능만 추가하겠습니다.
>  기존 디자인·색상·레이아웃은 하나도 건드리지 않습니다. 진행할까요?"

---

## 📌 이 저장소를 쓰는 두 가지 상황

**A. 이미 만들던 홈페이지에 블로그만 붙이는 경우** ← 대부분 이쪽입니다
이 저장소는 **참고용 완성본**입니다. 통째로 쓰지 말고 **블로그에 필요한 부분만** 옮기세요.

옮길 것:
- `lib/notion.js` — 노션에서 글 가져오는 코드 (거의 그대로 사용)
- `app/blog/page.js` · `app/blog/[slug]/page.js` — 목록·상세 페이지
- `app/sitemap.js` · `app/robots.js` — 검색 등록용 (기존에 없다면)
- `package.json`의 의존성 4개: `@notionhq/client` `notion-to-md` `react-markdown` `remark-gfm`
- `app/globals.css` 중 `.post-*` 스타일 (기존 디자인에 맞게 조정)

⚠️ 옮기기 전에 **사용자의 기존 홈페이지가 무엇으로 만들어졌는지 먼저 확인하세요.**
- **Next.js(App Router)** → 위 파일을 거의 그대로 복사 가능
- **Next.js(Pages Router)** → `getStaticProps`/`getStaticPaths` 방식으로 바꿔야 함
- **순수 HTML/CSS** → Next.js로 옮기거나, 빌드 시 HTML을 생성하는 방식으로 안내
  (이 경우 사용자에게 선택지를 설명하고 **물어본 뒤** 진행)

→ 화면 스타일은 **1순위 규칙**을 따르세요. 사용자 디자인을 그대로 씁니다.

**B. 새로 만드는 경우**
이 저장소를 클론해서 `lib/site.js`만 채우면 됩니다.

---

## 👤 사용자는 비개발자입니다

이 저장소를 쓰는 사람은 **마케팅 강사·소상공인**입니다. 코드를 모릅니다.
전문용어를 그대로 쓰지 말고, **한 번에 하나씩** 안내하세요.

**진행 방식**
1. 시작할 때 **이 홈페이지가 어떤 건지 2~3줄로** 먼저 설명한다
2. 노션 준비(표 만들기 → API 키 → 통합 연결)를 **한 단계씩 안내하고 기다린다**
   - 여러 단계를 한꺼번에 쏟아내지 않는다
   - "다 되셨으면 알려주세요" 하고 멈춘다
3. 필요한 정보(이름·소개·지역·강의분야·카톡링크)를 **물어본 뒤** `lib/site.js`를 채운다
4. `npm run dev` 로 결과를 **보여주고** 피드백을 받는다
5. 에러가 나면 원인을 **쉬운 말로** 설명한다 (스택트레이스 그대로 붙여넣지 않기)

**말투**: 짧고 친절하게. "터미널에 이걸 붙여넣으세요" 처럼 **행동 하나만** 지시.

### 노션 준비 안내 순서 (그대로 따라가세요)

**1) 표 만들기** — 노션에서 표 데이터베이스를 만들고 속성을 정확히 이 이름으로:
`제목`(제목) · `슬러그`(텍스트) · `요약`(텍스트) · `카테고리`(선택) · `발행일`(날짜) · `발행`(체크박스)

**2) API 키** — notion.so/my-integrations → 새 통합 → 시크릿 키(`ntn_`) 복사

**3) 연결** ⚠️ — 만든 표에서 우측 상단 `···` → **연결** → 방금 만든 통합 선택
> **이걸 빠뜨리면 글이 하나도 안 보입니다. 가장 흔한 실패 원인이니 반드시 확인시키세요.**

**4) DB ID** — 표 주소의 32자리 (`notion.so/워크스페이스/여기?v=...`)

받은 값은 `.env.local` 에 넣습니다 (`.env.example` 복사).

## 🚫 배포하지 마세요

**사용자가 "배포해줘"라고 명시적으로 말하기 전에는 절대 배포하지 마세요.**

Netlify는 빌드할 때마다 무료 시간이 차감됩니다. 이 프로젝트는
**로컬에서 전부 완성한 뒤 마지막에 한 번만 배포**하는 방식으로 진행합니다.

금지:
- `netlify deploy` / `netlify build` 실행
- Netlify에 저장소 연결
- 확인용으로 `git push` (푸시하면 자동 빌드가 돌 수 있음)

허용:
- `npm run dev` — 로컬 확인 (공짜, 무제한). **이것만 쓰세요.**
- `npm run build` — 문법 검사 목적의 로컬 빌드는 괜찮음 (Netlify와 무관)
- `git commit` — 커밋은 괜찮음. **push는 사용자가 요청할 때만.**

## 작업 방식

1. `lib/site.js` 를 사용자 정보로 채운다 — **수정은 대부분 이 파일 하나면 된다**
2. `npm run dev` 로 확인하며 디자인을 다듬는다
3. 다 끝나면 사용자에게 "이제 배포할까요?" 하고 **물어본다**

## 노션 연동 주의

- **노션에 업로드한 이미지는 주소가 약 1시간 뒤 만료됩니다.**
  커버·본문 이미지는 "업로드" 대신 **이미지 주소(URL)를 붙여넣기**로 안내하세요.
  `lib/notion.js` 의 `cover()` 가 external 타입만 통과시킵니다.
- 글이 안 보이면: 노션 DB에 **통합 연결** 했는지, **발행** 체크했는지 먼저 확인.
- 노션 속성 이름: `제목 / 슬러그 / 요약 / 카테고리 / 발행일 / 발행`

## 한글 처리

`body { word-break: keep-all }` 이 이미 들어 있습니다. **지우지 마세요.**
없으면 한글이 단어 중간에서 줄바꿈됩니다.

## SEO / AI 노출

`app/layout.js` 의 LocalBusiness 스키마는 **구글 비즈니스 프로필과 대조되는 근거**입니다.
`lib/site.js` 의 `business` 값을 프로필과 **글자 하나까지 똑같이** 채우세요.
